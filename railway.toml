[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn config.wsgi:application --chdir src --workers 2 --threads 2 --timeout 60 --bind 0.0.0.0:$PORT"
healthcheckPath = "/healthz/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
